import { fireEvent, render, screen } from "@testing-library/react"; import { describe, expect, it, vi } from "vitest"; import { GameSetupOverlayBase } from "./component"
describe("GameSetupOverlayBase", () => { it("offers friend mode and emits the exact mode", () => { const chooseMode = vi.fn(); render(<GameSetupOverlayBase isOpen state="mode" props={{ title: "Setup", gameTitle: "Pairs", supportsTeam: true, roomCode: "" }} on={{ chooseMode }} onDismiss={vi.fn()} />); fireEvent.click(screen.getByRole("button", { name: "Chơi cùng bạn" })); expect(chooseMode).toHaveBeenCalledWith("COUPLE") }) })
