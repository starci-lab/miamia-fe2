import { fireEvent, render, screen } from "@testing-library/react"; import { describe, expect, it, vi } from "vitest"; import { GameSetupOverlay } from "./index"
describe("GameSetupOverlay", () => { it("resolves create-room mode only after character choice", () => { const resolved = vi.fn(); render(<GameSetupOverlay game="match_pairs" isOpen onResolved={resolved} onCancelled={vi.fn()} />); fireEvent.click(screen.getByRole("button", { name: "Chơi cùng bạn" })); fireEvent.click(screen.getByRole("button", { name: "Tạo phòng mới" })); fireEvent.click(screen.getByRole("button", { name: "Chọn Mia" })); expect(resolved).toHaveBeenCalledWith({ game: "match_pairs", mode: "COUPLE", character: "MIA" }) }) })
