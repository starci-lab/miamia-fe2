import { render } from "@testing-library/react"; import { describe, expect, it } from "vitest"; import { _GameHubPage } from "./component"
describe("_GameHubPage", () => { it("keeps rivalry before catalog and swaps both for the runner", () => { const { rerender } = render(<_GameHubPage standingSurface={() => <>standing</>} catalogSurface={() => <>catalog</>} />); expect(document.body.textContent).toContain("standingcatalog"); rerender(<_GameHubPage standingSurface={() => <>standing</>} catalogSurface={() => <>catalog</>} runnerSurface={() => <>runner</>} />); expect(document.body.textContent).toContain("runner"); expect(document.body.textContent).not.toContain("catalog") }) })
