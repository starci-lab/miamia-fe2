import { render } from "@testing-library/react"; import { describe, expect, it } from "vitest"; import { GameHubPageBase } from "./component"
describe("GameHubPageBase", () => { it("keeps rivalry before catalog and swaps both for the runner", () => { const { rerender } = render(<GameHubPageBase standingSurface={() => <>standing</>} catalogSurface={() => <>catalog</>} />); expect(document.body.textContent).toContain("standingcatalog"); rerender(<GameHubPageBase standingSurface={() => <>standing</>} catalogSurface={() => <>catalog</>} runnerSurface={() => <>runner</>} />); expect(document.body.textContent).toContain("runner"); expect(document.body.textContent).not.toContain("catalog") }) })
