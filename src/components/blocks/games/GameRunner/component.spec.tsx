import { render, screen } from "@testing-library/react"; import { describe, expect, it } from "vitest"; import { GameRunnerBase } from "./component"
describe("GameRunnerBase", () => { it("shows a real room code and roster while waiting", () => { render(<GameRunnerBase state="waiting" props={{ gameType: "MATCH_PAIRS", character: "MIA", snapshot: { phase: "WAITING", gameType: "MATCH_PAIRS", mode: "COUPLE", remainingMs: 60000, round: 1, question: "", options: [], players: [{ id: "a", name: "Mia", character: "MIA", team: "", score: 0, combo: 0, hp: 3, progress: 0, action: "IDLE", bot: false }], winner: "", roomCode: "ROOM42" } }} />); expect(screen.getByText("Mã phòng: ROOM42")).toBeInTheDocument(); expect(screen.getByText("Mia")).toBeInTheDocument() }) })
it("renders connecting and finished states", () => { render(<GameRunnerBase state="connecting" props={{ gameType: "MATCH_PAIRS", character: "MIA" }} />); expect(screen.getByText("Đang kết nối server trò chơi…")).toBeInTheDocument() })
it("renders finished result ranking", () => { render(<GameRunnerBase state="finished" props={{ gameType: "MATCH_PAIRS", character: "MIA", snapshot: { phase: "FINISHED", gameType: "MATCH_PAIRS", mode: "COUPLE", remainingMs: 0, round: 1, question: "", options: [], players: [{ id: "a", name: "Mia", character: "MIA", team: "", score: 10, combo: 2, hp: 3, progress: 1, action: "IDLE", bot: false }], winner: "MIA", roomCode: "ROOM42" } }} />); expect(screen.getByText(/MIA chiến thắng/)).toBeInTheDocument() })
