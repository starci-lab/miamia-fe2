import { fireEvent, render, screen } from "@testing-library/react"; import { describe, expect, it, vi } from "vitest"; import { GameCatalogBase } from "./component"; import { GAME_CATALOG } from "@/modules/games/catalog"
describe("GameCatalogBase", () => { it("renders all four games and reports the selected identity", () => { const pick = vi.fn(); render(<GameCatalogBase state="ready" props={{ title: "Games", games: GAME_CATALOG, actionLabel: "Open" }} on={{ pick }} />); expect(screen.getAllByRole("button", { name: "Open" })).toHaveLength(4); fireEvent.click(screen.getAllByRole("button", { name: "Open" })[1]); expect(pick).toHaveBeenCalledWith("vocab_race") }) })
