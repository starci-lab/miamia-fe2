import { fireEvent, render, screen } from "@testing-library/react"; import { describe, expect, it, vi } from "vitest"; import { GameFriendStandingBase } from "./component"
describe("GameFriendStandingBase", () => { it("routes the guest CTA to authentication", () => { const requireSignIn = vi.fn(); render(<GameFriendStandingBase state="guest" props={{ title: "Friends", subtitle: "Play", ctaLabel: "Sign in" }} on={{ requireSignIn }} />); fireEvent.click(screen.getByRole("button", { name: "Sign in" })); expect(requireSignIn).toHaveBeenCalledOnce() }) })
