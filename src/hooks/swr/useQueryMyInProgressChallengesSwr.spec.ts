/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { setSessionToken } from "../auth/useSessionToken";import { useQueryMyInProgressChallengesSwr } from "./useQueryMyInProgressChallengesSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-my-in-progress-challenges",()=>({queryMyInProgressChallenges:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("viewer");m.query.mockReset();m.query.mockResolvedValue({data:{myInProgressChallenges:{data:[]}}})})
describe("useQueryMyInProgressChallengesSwr",()=>{it("stays idle for a guest",()=>{setSessionToken(undefined);const {result}=renderHook(()=>useQueryMyInProgressChallengesSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads resume challenges",async()=>{const {result}=renderHook(()=>useQueryMyInProgressChallengesSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual([]));expect(m.query).toHaveBeenCalledWith()})})
