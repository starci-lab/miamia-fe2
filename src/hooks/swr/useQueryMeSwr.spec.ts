/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { setSessionToken } from "../auth/useSessionToken";import { useQueryMeSwr } from "./useQueryMeSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-me",()=>({queryMe:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("viewer");m.query.mockReset();m.query.mockResolvedValue({data:{me:{data:{username:"ada"}}}})})
describe("useQueryMeSwr",()=>{it("stays idle for a guest",()=>{setSessionToken(undefined);const {result}=renderHook(()=>useQueryMeSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads the managed identity",async()=>{const {result}=renderHook(()=>useQueryMeSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({username:"ada"}));expect(m.query).toHaveBeenCalledWith()})})
