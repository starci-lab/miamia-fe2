/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { useQueryCourseMindMapSwr } from "./useQueryCourseMindMapSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-course-mind-map",()=>({queryCourseMindMap:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{courseMindMap:{data:{nodes:[]}}}})})
describe("useQueryCourseMindMapSwr",()=>{it("stays idle without course",()=>{const {result}=renderHook(()=>useQueryCourseMindMapSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads the graph",async()=>{const {result}=renderHook(()=>useQueryCourseMindMapSwr("course"),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({nodes:[]}));expect(m.query).toHaveBeenCalledWith({request:{courseId:"course"}})})})
