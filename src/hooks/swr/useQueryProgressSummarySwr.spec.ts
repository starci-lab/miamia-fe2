/** @vitest-environment jsdom */
import{createElement,type PropsWithChildren}from"react";import{renderHook,waitFor}from"@testing-library/react";import{SWRConfig}from"swr";import{beforeEach,describe,expect,it,vi}from"vitest";import{useQueryProgressSummarySwr}from"./useQueryProgressSummarySwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-progress-summary",()=>({queryProgressSummary:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{progressSummary:{data:{score:1}}}})});describe("useQueryProgressSummarySwr",()=>{it("idle when disabled",()=>{expect(renderHook(()=>useQueryProgressSummarySwr(false),{wrapper}).result.current.data).toBeUndefined()});it("reads summary",async()=>{const{result}=renderHook(()=>useQueryProgressSummarySwr(true),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({score:1}));expect(m.query).toHaveBeenCalledWith()})})
