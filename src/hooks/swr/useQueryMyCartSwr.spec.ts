/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { setSessionToken } from "../auth/useSessionToken";import { useQueryMyCartSwr } from "./useQueryMyCartSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-my-cart",()=>({queryMyCart:m.query,QueryMyCart:{Query1:"query-1"}}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("viewer");m.query.mockReset();m.query.mockResolvedValue({data:{myCart:{data:[]}}})})
describe("useQueryMyCartSwr",()=>{it("stays idle for a guest",()=>{setSessionToken(undefined);const {result}=renderHook(()=>useQueryMyCartSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads the cart with its query document",async()=>{const {result}=renderHook(()=>useQueryMyCartSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual([]));expect(m.query).toHaveBeenCalledWith({query:"query-1"})})})
