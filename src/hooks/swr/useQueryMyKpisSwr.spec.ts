/** @vitest-environment jsdom */
import {createElement,type PropsWithChildren} from "react";import {renderHook,waitFor} from "@testing-library/react";import {SWRConfig} from "swr";import {beforeEach,describe,expect,it,vi} from "vitest";import {setSessionToken} from "../auth/useSessionToken";import {useQueryMyKpisSwr} from "./useQueryMyKpisSwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-my-kpis",()=>({queryMyKpis:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("v");m.query.mockReset();m.query.mockResolvedValue({data:{myKpis:{data:{score:1}}}})});describe("useQueryMyKpisSwr",()=>{it("idle guest",()=>{setSessionToken(undefined);expect(renderHook(()=>useQueryMyKpisSwr(),{wrapper}).result.current.data).toBeUndefined()});it("reads kpis",async()=>{const {result}=renderHook(()=>useQueryMyKpisSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({score:1}));expect(m.query).toHaveBeenCalledWith()})})
