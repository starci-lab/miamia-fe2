/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { useQueryCourseReviewsSwr } from "./useQueryCourseReviewsSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-course-reviews",()=>({queryCourseReviews:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{courseReviews:{data:{count:0,data:[]}}}})})
describe("useQueryCourseReviewsSwr",()=>{it("stays idle without course",()=>{const {result}=renderHook(()=>useQueryCourseReviewsSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads course reviews",async()=>{const {result}=renderHook(()=>useQueryCourseReviewsSwr("course"),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({count:0,data:[]}));expect(m.query).toHaveBeenCalledWith({request:{courseId:"course"}})})})
