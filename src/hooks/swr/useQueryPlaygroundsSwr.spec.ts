/** @vitest-environment jsdom */
import{createElement,type PropsWithChildren}from"react";import{renderHook,waitFor}from"@testing-library/react";import{SWRConfig}from"swr";import{beforeEach,describe,expect,it,vi}from"vitest";import{useQueryPlaygroundsSwr}from"./useQueryPlaygroundsSwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-playgrounds",()=>({queryPlaygrounds:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{playgrounds:{data:[]}}})});describe("useQueryPlaygroundsSwr",()=>{it("idle without course",()=>{expect(renderHook(()=>useQueryPlaygroundsSwr(),{wrapper}).result.current.data).toBeUndefined()});it("reads catalog",async()=>{const{result}=renderHook(()=>useQueryPlaygroundsSwr("course"),{wrapper});await waitFor(()=>expect(result.current.data).toEqual([]));expect(m.query).toHaveBeenCalledWith({courseId:"course"})})})
