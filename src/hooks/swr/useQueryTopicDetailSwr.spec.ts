/** @vitest-environment jsdom */
import{createElement,type PropsWithChildren}from"react";import{renderHook,waitFor}from"@testing-library/react";import{SWRConfig}from"swr";import{beforeEach,describe,expect,it,vi}from"vitest";import{useQueryTopicDetailSwr}from"./useQueryTopicDetailSwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-topic-detail",()=>({queryTopicDetail:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{topicDetail:{data:{slug:"topic"}}}})});describe("useQueryTopicDetailSwr",()=>{it("idle empty slug",()=>{expect(renderHook(()=>useQueryTopicDetailSwr(""),{wrapper}).result.current.data).toBeUndefined()});it("reads topic",async()=>{const{result}=renderHook(()=>useQueryTopicDetailSwr("topic"),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({slug:"topic"}));expect(m.query).toHaveBeenCalledWith("topic")})})
