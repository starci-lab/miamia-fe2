/** @vitest-environment jsdom */
import { createElement,type PropsWithChildren } from "react";import { renderHook,waitFor } from "@testing-library/react";import { SWRConfig } from "swr";import { beforeEach,describe,expect,it,vi } from "vitest";import { useQueryPaperDetailSwr } from "./useQueryPaperDetailSwr"
const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("@/modules/api/graphql/queries/query-paper-detail",()=>({queryPaperDetail:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{m.query.mockReset();m.query.mockResolvedValue({data:{paperDetail:{data:{slug:"paper"}}}})})
describe("useQueryPaperDetailSwr",()=>{it("stays idle without slug",()=>{const {result}=renderHook(()=>useQueryPaperDetailSwr(),{wrapper});expect(result.current.data).toBeUndefined()});it("reads paper by slug",async()=>{const {result}=renderHook(()=>useQueryPaperDetailSwr("paper"),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({slug:"paper"}));expect(m.query).toHaveBeenCalledWith("paper")})})
