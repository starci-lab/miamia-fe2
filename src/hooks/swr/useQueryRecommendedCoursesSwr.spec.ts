/** @vitest-environment jsdom */
import{createElement,type PropsWithChildren}from"react";import{renderHook,waitFor}from"@testing-library/react";import{SWRConfig}from"swr";import{beforeEach,describe,expect,it,vi}from"vitest";import{setSessionToken}from"../auth/useSessionToken";import{useQueryRecommendedCoursesSwr}from"./useQueryRecommendedCoursesSwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-recommended-courses",()=>({queryRecommendedCourses:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("v");m.query.mockReset();m.query.mockResolvedValue({data:{recommendedCourses:{data:{items:[]}}}})});describe("useQueryRecommendedCoursesSwr",()=>{it("idle for guest",()=>{setSessionToken(undefined);expect(renderHook(()=>useQueryRecommendedCoursesSwr(),{wrapper}).result.current.data).toBeUndefined()});it("reads recommendations",async()=>{const{result}=renderHook(()=>useQueryRecommendedCoursesSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual([]));expect(m.query).toHaveBeenCalledWith()})})
