/** @vitest-environment jsdom */
import {createElement,type PropsWithChildren} from "react";import {renderHook,waitFor} from "@testing-library/react";import {SWRConfig} from "swr";import {beforeEach,describe,expect,it,vi} from "vitest";import {setSessionToken} from "../auth/useSessionToken";import {useQueryMyLeagueSwr} from "./useQueryMyLeagueSwr";const m=vi.hoisted(()=>({query:vi.fn()}));vi.mock("../../modules/api/graphql/queries/query-my-league",()=>({queryMyLeague:m.query}));const wrapper=({children}:PropsWithChildren)=>createElement(SWRConfig,{value:{provider:()=>new Map(),dedupingInterval:0}},children);beforeEach(()=>{setSessionToken("v");m.query.mockReset();m.query.mockResolvedValue({data:{myLeague:{data:{rank:1}}}})});describe("useQueryMyLeagueSwr",()=>{it("idle guest",()=>{setSessionToken(undefined);expect(renderHook(()=>useQueryMyLeagueSwr(),{wrapper}).result.current.data).toBeUndefined()});it("reads league",async()=>{const {result}=renderHook(()=>useQueryMyLeagueSwr(),{wrapper});await waitFor(()=>expect(result.current.data).toEqual({rank:1}));expect(m.query).toHaveBeenCalledWith()})})
